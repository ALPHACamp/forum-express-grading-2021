<h1>餐廳後台</h1>
<div class="mb-3">
  <a href="/admin/restaurants">Restaurants</a>｜<a href="/admin/users">Users</a>
</div>

<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Email</th>
      <th scope="col">Role</th>
      <th scope="col">#</th>
    </tr>
  </thead>
  <tbody>
    {{#each users}}
    <tr>
      <th scope="row">{{this.id}}</th>
      <td>{{this.email}}</td>
      <td>{{{showRole this.isAdmin}}}</td>
      <td>
        <button type="submit" class="btn btn-link">{{{setRole this.isAdmin}}}</button>
        {{!-- <form action="/admin/restaurants/{{this.id}}?_method=DELETE" method="POST" style="display: inline;">
          <button type="submit" class="btn btn-link">Delete</button>
        </form> --}}
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>